DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/cloudbreak
REDIS_URL=redis://localhost:6379
WEATHER_API_KEY=
SUPABASE_URL=
SUPABASE_KEY=
POSTHOG_API_KEY=
EXPO_ACCESS_TOKEN=
SUPABASE_SERVICE_ROLE_KEY=  # Supabase Dashboard → Settings → API → service_role secret (jamais exposé côté client)
ENVIRONMENT=development
LEGAL_BASE_URL=https://api.cloudbreak.fr  # URLs légales servies via /legal (privacy, cgu) — mettre à jour avant release
